# Get a key from Google Cloud Console → APIs & Services → Credentials
# Enable "YouTube Data API v3" on the project first.
# Restrict the key by HTTP referrer to your prod domain + localhost.
VITE_YOUTUBE_API_KEY=your_api_key_here
